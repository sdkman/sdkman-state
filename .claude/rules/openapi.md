# OpenAPI Documentation

Any change to API endpoints — new routes, modified request/response schemas, new query parameters, changed status codes — **must** include a corresponding update to `src/main/resources/openapi/documentation.yaml`.

The OpenAPI spec is the contract for API consumers. Keeping it in sync with the implementation is not optional.
